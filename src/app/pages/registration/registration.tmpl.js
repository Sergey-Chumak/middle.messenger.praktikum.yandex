export const tmpl = `
    <div class="registration">
        {{>email }}
        {{>login }}
        {{>name }}
        {{>lastName }}
        {{>phone }}
        {{>password }}
        {{>repeatPassword }}
        {{>submitBtn }}
    </div>
`;
