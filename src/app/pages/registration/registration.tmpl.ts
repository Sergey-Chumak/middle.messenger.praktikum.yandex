export const tmpl = `
    <div class="registration">
        {{{ emailInput }}}
        {{{ loginInput }}}
        {{{ nameInput }}}
        {{{ lastNameInput }}}
        {{{ phoneInput }}}
        {{{ passwordInput }}}
        {{{ passwordRepeatInput }}}
        {{{ submitBtn }}}
        <a href="/auth" class="registration__link">I have a profile</a>
    </div>
`;
