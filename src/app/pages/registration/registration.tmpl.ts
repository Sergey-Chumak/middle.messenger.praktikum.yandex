export const tmpl = `
    <div class="registration">
        {{{ emailInput }}}
        {{{ loginInput }}}
        {{{ nameInput }}}
        {{{ lastNameInput }}}
        {{{ phoneInput }}}
        {{{ passwordInput }}}
        {{{ passwordRepeatInput }}}
        {{{ submitBtn }}}
        <a id="registration-link" class="registration__link">I have a profile</a>
    </div>
`;
