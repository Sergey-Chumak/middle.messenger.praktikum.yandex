export const tmpl = `
    <div class="auth">
    <form id="sign-in-form">
        {{{ loginInput }}}
        {{{ passwordInput }}}
        {{{ button }}}
    </form>
    <a class="auth__link" href="/registration">Create account</a>
    </div>
`;
